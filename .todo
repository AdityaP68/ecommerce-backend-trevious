#caching 
#rate limiting 
#redis lookup table for token blacklisting
#typescript refactor (composition pattern ??)
#using prisma (also checkout the prisma bottleneck conditions)
#adding streams for product intro
#containerization + CI/CD (github actions) setup

